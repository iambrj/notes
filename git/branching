git branch <branch name> #create a pointer named branch name
git log --oneline --decorate #show current branch
git checkout <branch name> #switch to branch name
git log --oneline --decorate --graph --all #show branch history
git checkout -b <branch name> #create and switch to <branch name>
git merge <branch name> #merge current branch with <branch name>
git branch -d <branch name> #delete branch name
git mergetool #fires up a graphical git merging tool
git branch #displays all branches with * on HEAD branch
git branch -v #displays all branches with last commit
git branch --merged #displays all merged branches that are safe to be deleted
git branch --no-merge #displays all unmerged branches
git branch -D <branch name> #delete an unmerged branch
git branch --no-merge <branch name> #what is not merged in <branch name>
Remote branches are manages as <remote>/<branch>
git fetch <remote> #sync repo
git push <remote> <branch>
git checkout --track <remote>/<branch>
git branch -vv #detailed info about tracking branches
git pull #download info from currently tracking branch
git push <short> --delete <branch>
git remote add <short> <url>
git rebase <branch> #rebase current branch onto <branch>

